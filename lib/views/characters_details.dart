import 'package:flutter/material.dart';

import 'allCharacters/models/character.dart';

class CharacterDetails extends StatelessWidget {
  final  character;

  const CharacterDetails({this.character});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
